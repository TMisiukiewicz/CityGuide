import React from 'react';
import { View } from 'react-native';
import { SingleObject } from 'types';

export default function ObjectCard(props: SingleObject): JSX.Element {
    return (
        <View>
            
        </View>
    )
}
